# AI Reverse-Invention Generator Environment Variables

# Required: OpenAI API Key
OPENAI_API_KEY=your_openai_api_key_here

# Optional: GPT Model (default: gpt-4o-mini)
GPT_MODEL=gpt-4o-mini

# Optional: Image Model (default: dall-e-3)
IMAGE_MODEL=dall-e-3

# Optional: Port (default: 8080)
PORT=8080

# Optional: Node Environment
NODE_ENV=production

# Optional: Neo4j Configuration (for advanced features)
NEO4J_URI=neo4j+s://your-neo4j-instance.databases.neo4j.io
NEO4J_USER=neo4j
NEO4J_PASSWORD=your_password_here

# Note: In Replit, add these as secrets instead of using .env file